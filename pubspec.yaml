name: beleg_speicher
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.0.15
  image_picker: ^0.8.7+4
  file_selector: ^0.9.0
  table_calendar: ^3.0.8
  intl: ^0.18.0
  flutter_secure_storage: ^8.0.0
  open_file: ^3.2.1
  share_plus: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/Pfeil_Back.png
    - assets/calendar_home.png
    - assets/folder_home.png
    - assets/time_home.png
